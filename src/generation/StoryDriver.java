package generation;

import generation.QuadNode;
import generation.QuadTree;

public class StoryDriver
{
   public static void main(String[] arg)
   {
   QuadNode parent = new QuadNode(null);
   QuadTree storyline = new QuadTree();
   //storyline.createNewSet();
   }
}